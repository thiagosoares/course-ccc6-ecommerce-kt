package br.com.tfsoares.courses.branas.ecommerce.domain.entity

import org.junit.jupiter.api.Test

class OrderItemTest {

    @Test
    fun `GIVEN , WHEN , THEN `() {
    }

    fun `GIVEN a valid input, WHEN create a Order item, THEN create a valid order item`() {
//        const orderItem = new OrderItem(1, 1000, 2);
//        assertThat(orderItem.getTotal()).toBe(2000);
    }
}
